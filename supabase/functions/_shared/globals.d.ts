import { SupabaseClient } from 'https://esm.sh/@supabase/supabase-js@2.7.1'

/**
 * This setup allows for a globally accessible Supabase client,
 * which simplifies client management and makes the code more modular and maintainable.
 */
/*declare global {
    var supabaseClient: SupabaseClient
}

export {}*/
